<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Cor de fundo -->
    <solid android:color="#1150ab" />

    <!-- Bordas arredondadas -->
    <corners android:radius="16dp" />

    <!-- Borda (opcional) -->
    <stroke
        android:width="2dp"
        android:color="#1150ab" />
</shape>
